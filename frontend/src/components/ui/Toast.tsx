'use client';
// Toast is rendered by ToastProvider in context — this is a no-op placeholder
// for explicit imports if needed. The actual toast div lives in ToastContext.
export default function Toast() {
  return null;
}
